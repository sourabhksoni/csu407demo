# Model B. Same base LLM, same prompt, same eval set, adapter attached.
name: adapted
base_model: null            # set in Phase 2, licence checked for fine tuning first
adapter_path: models/adapters/cg-agri-extract-v1
eval_set: data/eval/extraction_eval.jsonl
prompt_file: models/configs/extraction_prompt.txt

schema_fields:
  - crop
  - district
  - season
  - year
  - area
  - production
  - yield
  - unit

numeric_fields:
  - area
  - production
  - yield

generation:
  temperature: 0.0
  max_new_tokens: 512
